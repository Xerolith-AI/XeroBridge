buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Downgrade gradle plugin to match older stable JVM/Gradle versions
        classpath 'com.android.tools.build:gradle:7.4.2'
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
